extends layout

block content
  h1= title
  p Welcome to #{title}
  .map
    canvas.layer#over(style="z-index:0")
      | Dein Browser kann diese Grafik nicht darstellen.
    canvas.layer#events(style="z-index:1")
      | Dein Browser kann diese Grafik nicht darstellen.
    canvas.layer#over(style="z-index:2")
      | Dein Browser kann diese Grafik nicht darstellen.